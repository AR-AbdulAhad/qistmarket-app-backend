// src/utils/officerVerificationActiveMap.js
// Shared in-memory map to track which officer is active in a verification screen
const officerVerificationActiveMap = new Map();
module.exports = officerVerificationActiveMap;
